<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ButtonThemeAction" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/actions_db_button</item>
        <item name="android:textColor">@drawable/actions_button_textcolor</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">17sp</item>
    </style>

    <style name="MyProgressBar" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/custom_progressbar</item>
        <item name="android:minHeight">80dp</item>
        <item name="android:minWidth">200dp</item>
    </style>

</resources>
